@AGENTS.md

> 위 줄은 Expo 57 버전 문서 경고를 불러온다. 지우지 않는다.
> 세션별 기능 프롬프트는 `prompts/`에 있다. 상세 근거는 `../Connecting-app/design-research/`.

# CLAUDE.md — MediVU Connecting App UI·UX 디자인 규칙 (프로젝트 적용본)

작성일: 2026-09-04
출처: `../Connecting-app/design-research/Claude/` · `../Connecting-app/design-research/Codex/` · `../Connecting-app/design-research/Gemini/` 3개 조사본 통합
적용 대상: 이 저장소에서 UI를 만들거나 고치는 모든 작업(사람·LLM 에이전트 공통)

---

## 0. 이 문서의 지위 — 먼저 읽을 것

1. **이 문서가 프로젝트의 정본이다.** `../Connecting-app/design-research/`의 원본 3종은 근거·상세이고, 원본끼리 충돌하는 지점은 §4에서 이미 해결했다. 원본이 서로 다를 때 이 문서를 따른다.
2. **여기 규칙은 전부 가설이다.** 표준·플랫폼 문서·해외 연구·국내 통계에 근거하지만 **실제 대상 사용자 테스트를 통과한 규칙은 0건이다.**
3. 자동 검사 통과나 LLM 검토를 "사용성 검증됨"으로 보고하지 않는다. 검증은 항상 `AUTO`(자동) / `DEV`(개발자 수동) / `USER`(실사용자)를 구분해 적는다.
4. 수치를 인용하거나 "표준을 지켰다"고 쓰기 전에 `../Connecting-app/design-research/Claude/SOURCES.md`에서 확인 수준(`[A]`/`[B]`/`[C]`)을 확인한다. `[C]`는 원문 미확인이다.
5. **표준 요구 / 플랫폼 권고 / 이 프로젝트 설계값**을 구분해 표기한다. 설계값을 표준이라고 쓰지 않는다.
6. 사용자 전역 지침(`~/.claude/CLAUDE.md`)과 충돌하면 전역 지침이 우선한다. 이 문서는 그 위에 **UI 도메인 규칙만** 얹는다.

### 강도 표기

| 표기 | 의미 |
|---|---|
| **MUST** | 위반하면 출시 차단 |
| **SHOULD** | 기본값. 어기려면 근거를 코드 주석 또는 설계 문서에 기록 |
| **MAY** | 선택 |

### 조건부 표기

`[의료]` 진료·처방·예약·건강데이터 흐름에만 / `[KR]` 한국 서비스에만 / `[웹]` `[iOS]` `[AOS]` 해당 플랫폼에만

---

## 1. 프로젝트 전제

### 1.1 대상 사용자

디지털 친숙도가 평균 이하인 한국 사용자 — 환자, 고령층, 보호자, 산간벽지·도서 지역 거주자.

- **나이·지역으로 능력이나 환경을 단정하지 않는다.** 디지털 숙련도 / 읽기 / 시각·청각·손 조작 / 피로·통증 / 기기·통신을 **각각 별도로** 확인한다.
- 확인 안 된 전제는 **"가정"으로 표시**하고, 가정마다 검증 방법과 담당자를 적는다.
- **크기를 키우는 것이 답이 아니다.** 복잡도가 크기보다 사용성을 더 좌우한다. 일괄 확대·일괄 단순화·유아적 문체("눌러보세용")는 전부 실패다.
- 환자·보호자는 **인지 대역폭이 좁아진 상태**로 앱을 연다. 팝업 광고·복잡한 인증·의미 없는 오류 코드는 즉시 포기로 이어진다.

### 1.2 이 앱은 `[의료]` 앱이다 — 판정 완료

진료 신청·예약, 의료진 대상 문의 채팅, 건강 정보 표시, 보호자 대리 이용이 모두 범위에 있다.
→ **MED-01~04, PRX-01~02 규칙이 항상 적용된다.** 이 판정을 뒤집으려면 기능 범위 변경 결정을 문서에 남겨야 한다.

### 1.3 성공의 정의

성공은 체류 시간·접속 빈도·중독성이 **아니다.**

**독립 과업 성공률 / 신뢰 / 부담 감소 / 서비스 필요 주기에 맞춘 재사용**이다.
체류 시간을 늘리는 다크 패턴, 불필요한 알림 증가, 연속 출석 유도는 이 프로젝트에서 **금지**한다.

### 1.4 안전 우선 원칙 (CORE-15)

**안전·접근성·핵심 과업을 막는 문제는 시각적 완성도로 상쇄할 수 없다.**
디자인 완성도·애니메이션 품질·브랜드 일관성 점수로 출시 게이트 G1~G4를 통과시키지 않는다.

---

## 2. 항상 지키는 15가지 (CORE)

작업 시작 전에 매번 확인한다. 상세 규칙과 충돌하면 상세 규칙이 우선한다.

| ID | 규칙 | 강도 |
|---|---|---|
| CORE-01 | 나이·지역으로 능력을 단정하지 않는다. 미확인 전제는 **가정으로 명시**한다 | MUST |
| CORE-02 | 한 화면에서는 **하나의 결정**을 요구한다. 묶으면 근거를 기록한다 | SHOULD |
| CORE-03 | 기본값을 대상 사용자 기준으로 잡는다. **접근성 설정을 켜야만 쓸 만한 화면은 실패다** | SHOULD |
| CORE-04 | 익숙한 것을 새로 만들지 않는다. 플랫폼 표준 컴포넌트·제스처·위치를 먼저 쓴다 | SHOULD |
| CORE-05 | 누른 즉시 눌림 반응. **입력 확인과 작업 완료는 다른 신호**로 알린다 | MUST |
| CORE-06 | 뒤로 가기·앱 종료·재연결 후에도 진행 중 작업과 입력값이 남는다 | MUST |
| CORE-07 | 취소·수정·복구를 항상 준다. **다운 이벤트만으로 실행하지 않는다** | MUST |
| CORE-08 | 색·소리·모션 **단독으로** 의미를 전달하지 않는다 | MUST |
| CORE-09 | 크기를 키우기 전에 **복잡도**를 줄인다 | SHOULD |
| CORE-10 | 저장·전송·접수·확정·실패를 **다른 단어**로 구분하고 **시각을 붙인다** | MUST |
| CORE-11 | 이미 받은 정보를 다시 묻지 않는다. 인증이 암기에만 의존하지 않게 한다 | MUST |
| CORE-12 | 오프라인·느린 통신·강제 종료는 **정상 경로**다 | MUST |
| CORE-13 | 중복 실행은 사용자가 아니라 **시스템**이 막는다(멱등 키) | MUST |
| CORE-14 | 보호자 대리 이용은 **1급 시나리오**다. 누구의 정보인지 항상 화면에 있다 | MUST |
| CORE-15 | 안전·접근성·핵심 과업 문제는 **시각 완성도로 상쇄할 수 없다** | MUST |

---

## 3. 상세 문서를 읽는 조건

**상세 문서가 자동으로 읽힌다고 가정하지 말 것.** 아래에 해당하면 **작업 시작 전에** 연다. 해당하지 않으면 열지 않는다. 전체를 미리 읽지 않는다.

> 표 안의 짧은 파일명은 전부 `../Connecting-app/design-research/Claude/` 아래에 있다.

| 상황 | 읽을 문서 | 범위 |
|---|---|---|
| 새 화면·새 흐름 설계 | `../Connecting-app/design-research/Claude/DESIGN_RULES.md` | §1 CORE + 해당 영역 |
| 폼·입력·오류 | `DESIGN_RULES.md` | FRM-01~04, TXT-03~04 |
| 로딩·실패·오프라인·동기화 | `DESIGN_RULES.md` | STA-01~04, SYN-01~02 |
| 애니메이션·전환·성능 | `DESIGN_RULES.md` | MOT-01~03 |
| 색·크기·간격·터치 영역 | `DESIGN_RULES.md` + `COMPONENT_AND_STATE_GUIDE.md` | VIS-01~04 + §2 |
| 컴포넌트 신규·상태 추가 | `COMPONENT_AND_STATE_GUIDE.md` | §1 상태 모델 + §3 |
| 문구 작성 | `COMPONENT_AND_STATE_GUIDE.md` | §4 |
| 가입·로그인·권한·동의 | `DESIGN_RULES.md` | ACC-01~03 |
| **진료·처방·예약·건강데이터** | `DESIGN_RULES.md` | **MED-01~04, PRX-01~02 (필수)** |
| 한국어 문구·단위·플랫폼 차이 심화 | `../Connecting-app/design-research/Codex/KOREAN-MOBILE-UX-RESEARCH.md` | §4.2 단위, §5.1 컴포넌트 계약, §5.2 승인형 상태 모델, §5.3 문구 |
| 수치 인용·"표준 준수" 주장 | `../Connecting-app/design-research/Claude/SOURCES.md` | 해당 출처 확인 수준 |
| 검수·출시 판정 | `../Connecting-app/design-research/Claude/DESIGN_QA_CHECKLIST.md` | 해당 단계 |
| 기존 화면 후반 개선 | `../Connecting-app/design-research/Claude/APPLICATION_PROMPTS.md` | 절차 B |

> `../Connecting-app/design-research/Gemini/포용적 앱 디자인 개발 지침.docx`는 **바이너리라 에이전트가 직접 읽기 어렵다.** 핵심 내용(15대 규칙, 세부 규칙 T/L/C/I/H 계열)은 이 문서에 반영했다. 원문이 필요하면 사람이 텍스트로 변환해 둘 것.

---

## 4. 조사본 충돌 해결 — 확정 사항

세 조사본이 서로 다르게 말한 지점이다. **아래가 이 프로젝트의 결정이며, 원본을 다시 읽고 뒤집지 않는다.**

| # | 쟁점 | Claude 조사본 | Codex 조사본 | Gemini 조사본 | **확정** |
|---|---|---|---|---|---|
| C-1 | 진행 표시(스텝 인디케이터) | 근거 있을 때만. 12단계 표시기를 제거해도 완료율이 변하지 않은 실서비스 사례 | "클릭 최소"·"한 화면 하나"를 절대 규칙화 금지 | 2화면 이상이면 **항상** `1/3` 표시 | **`[의료]` 진료 신청·예약·처방 흐름은 남은 단계를 표시한다**(중단 손실이 크다). 그 외 흐름은 5단계 이상이거나 단계 수가 가변일 때만. 형태는 **그래픽 바가 아니라 텍스트**(`2/4 · 연락처`). 분기가 있으면 총 단계 수를 말하지 않는다 |
| C-2 | 본문 기본 크기 | 17pt / 17sp / 1.0625rem (설계값) | iOS 17→18pt, AOS 18sp, 웹 1.125rem (시험 시작값) | iOS 최소 11pt·권장 17pt, AOS 최소 14sp·권장 16sp | **하한을 17pt / 17sp / 1.0625rem으로 두고 시작한다.** 18로 올릴지는 실기기 시험 후 결정 → §12-1. 어느 쪽이든 **설계값이지 표준 요구가 아니다** |
| C-3 | 모션 지속 시간 | ms를 정답처럼 선언 금지. 저성능 기준 기기에서 맞출 것 | 커스텀 전환 150–250ms부터 실험, 눌림 100ms 이내 예산 | 200–300ms 고정 | **고정값을 코드에 박지 않는다.** 150–300ms를 **시험 시작 범위**로 쓰고, 기준 저성능 기기에서 체감을 맞춘 뒤 토큰에 기록한다. 기준 기기 미지정 → §12-2 |
| C-4 | 확인 대화상자 | 확인이 필요한 액션 **목록을 문서화**하고 그 외엔 넣지 않는다(남발하면 안 읽는다) | 위험·복구 가능성으로 결정, 무조건 늘리지 않는다 | 파괴적 액션에 **반드시** 재확인 | **목록 방식 채택.** 목록: 처방 전송 / 예약 확정 / 예약 취소 / 제3자 제공 / 진료 기록 공유 / 계정 연결 해제. 별표 해제 같은 가벼운 행동에는 넣지 않는다 |
| C-5 | 대리 모드 구분 | 지속 배너 + 텍스트. **색만으로 구분 금지** | 정보 주체·수행자·권한 범위가 화면·알림·딥링크·초안 전반에서 일치 | 글로벌 헤더 **색상 변경** + 배너 | **배너 + 대상자 이름 텍스트가 필수.** 색상 변경은 보조로만 허용하고, 회색조로 바꿔도 구분이 되어야 한다 |
| C-6 | 낙관적 UI | `[의료]` 안전 관련 값에 **금지**. 되돌릴 수 있는 범위에서만 | 저위험·되돌릴 수 있는 행동에 한정 | 일반 데이터는 사용, 의료는 배제 | **허용 목록 방식.** 허용: 정렬·필터·별표(즐겨찾기)·읽음 표시. **금지: 예약 확정·처방 전송·복약 기록·검사 수치·제3자 제공 여부.** 금지 목록은 정적 검사로 강제 |
| C-7 | 로딩 표시 | 무기한 스피너·가짜 진행률 금지. 무엇을 기다리는지 문장으로 | 인위적 대기 없음, 중단 가능 | 1초 이상이면 스켈레톤 또는 프로그레스 바 | **스켈레톤 채택**(실제 콘텐츠와 같은 높이). **가짜 퍼센트 진행률 금지.** 문구는 대상 + 진행형(`예약 내역을 불러오는 중`) |
| C-8 | KS X 3253 판본 | 2016판, **원문 미확인** | **2025-12-31 개정판** 확인(4원칙·13지침·41검사항목) | 언급 없음 | **2025 개정판을 기준으로 본다.** 단 원문 대조를 하지 않았으므로 "KS 준수"라고 쓰지 않는다 → §12-3 |

---

## 5. 디자인 토큰

### 5.1 2층 구조 — 강제

```
[1층] 의미 토큰   ← 코드는 이 이름만 쓴다 (정본)
[2층] 플랫폼 매핑 ← iOS pt / Android dp·sp / Web rem·CSS px
```

**코드에 숫자 리터럴을 넣지 않는다.** 린트로 차단한다.
**단위를 섞지 않는다**: iOS `pt` / Android `dp`·`sp` / Web `rem`·`CSS px`. 같은 문장에서 두 단위를 함께 쓰지 않는다. CSS pt와 iOS pt는 다른 단위다. 스크린샷의 물리 픽셀로 비교하지 않는다.

### 5.2 타이포그래피

| 의미 토큰 | 역할 | iOS | Android | Web | 확대 상한 |
|---|---|---|---|---|---|
| `text.display` | 화면 대표 제목 | `.largeTitle` | 28sp / lh 36sp | 1.75rem / 1.3 | 1.4× |
| `text.title` | 섹션 제목 | `.title2` | 22sp / 30sp | 1.375rem / 1.35 | 1.6× |
| `text.headline` | 카드 제목 | `.headline` | 19sp / 26sp | 1.1875rem / 1.4 | 1.8× |
| `text.body` | **본문 기본** | `.body` | 17sp / 26sp | 1.0625rem / 1.55 | **상한 없음** |
| `text.bodyStrong` | 본문 강조 | `.body` + semibold | 17sp / 26sp + 600 | 1.0625rem + 600 | 상한 없음 |
| `text.label` | 버튼·입력 라벨 | `.body` | 17sp / 24sp | 1.0625rem / 1.4 | 상한 없음 |
| `text.caption` | 보조(시각·단위) | `.subheadline` | 15sp / 22sp | 0.9375rem / 1.45 | 상한 없음 |
| `text.legal` | 법적 고지 | `.footnote` | 13sp / 20sp | 0.8125rem / 1.5 | 상한 없음 |

- 본문·라벨에 **확대 상한을 두지 않는다**(두면 200% 확대 요구 위반). 상한은 큰 제목 토큰에만.
- 굵기 Regular(400) 이상. **Light/Thin 금지.**
- `[iOS]` Dynamic Type 텍스트 스타일 사용. 고정 pt 하드코딩 금지.
- `[AOS]` 글자 크기와 **`lineHeight`를 둘 다 `sp`** 로. **패딩·높이에 `sp` 금지**(Android 14+ 비선형 확대로 `4sp + 20sp ≠ 24sp`). 고정 배수로 sp를 환산하지 않는다.
- `[웹]` `rem`. `px` 고정 금지.
- `text.caption`보다 작은 토큰을 새로 만들지 않는다. 작은 글자가 필요하다는 신호는 **정보를 줄이라는 신호**다.
- 한국어 본문 행간 **1.5~1.7**. 한 줄 길이 목표 **한국어 25~40자**.
- `[웹]` 본문에 `word-break: keep-all;` + `overflow-wrap: anywhere;` **둘 다** 적용(어절 보호 + 긴 URL·계좌번호 넘침 방지). 둘이 충돌하면 **넘침 방지가 우선**한다.
- 버튼 라벨에 말줄임(`…`) 금지. 라벨이 길면 **자르지 말고 라벨을 고친다.** 2줄까지 허용.
- 문구에 강제 개행(`\n`) 금지 — 확대·화면 폭 변화에서 깨진다.
- 이미지에 텍스트를 굽지 않는다(확대·스크린리더 모두 실패).
- 텍스트 컴포넌트에 **고정 높이 금지.** 세로 확장을 허용한다.

### 5.3 색

| 의미 토큰 | 대비 요구 |
|---|---|
| `color.text.primary` | **목표 7:1**, 최소 4.5:1 |
| `color.text.secondary` | 최소 4.5:1 (3:1로 낮추지 않는다) |
| `color.text.onAction` | 버튼 배경 대비 최소 4.5:1 |
| `color.border.control` | 인접 배경 대비 최소 3:1 |
| `color.state.success` / `warning` / `danger` / `pending` | **색 단독 사용 금지** — 아이콘(모양) + 텍스트 필수 |
| `color.focus` | 배경 대비 최소 3:1, 두께 2 이상 |

- **표준 최소**: 본문 4.5:1 / 큰 텍스트(18pt 또는 굵은 14pt 이상) 3:1 / UI 컴포넌트·그래픽 경계 3:1.
- **이 프로젝트 목표(설계값)**: 본문 7:1 이상, 보조 텍스트도 4.5:1 이상.
- **라이트·다크 각각 측정**한다. 반올림으로 합격 처리 금지. 시안이 아니라 실제 렌더링에서 확인한다.
- 기준 미달 조합은 **사용 금지 목록**에 등록하고 자동 검사로 차단한다.
- 브랜드 색이 기준 미달이면 **텍스트에 쓰지 않고** 배경·강조에만 쓴다.
- 비활성 컨트롤도 **무엇인지 읽을 수는** 있어야 한다.
- 반투명 오버레이 위 텍스트 금지(배경에 따라 대비가 달라진다).
- 색 HEX·모서리 반경·그림자·서체를 공통 정답으로 정하지 않는다. 역할 토큰을 먼저 정하고 실제 조합을 검증한다.

### 5.4 간격·크기

| 의미 토큰 | 값 (iOS pt / AOS dp / Web CSS px — 동일 수치) | 용도 |
|---|---|---|
| `space.xs` | 4 | 아이콘–라벨 |
| `space.sm` | 8 | **인접 터치 타깃 최소 간격** |
| `space.md` | 12 | **파괴적 액션 분리 최소 간격** |
| `space.lg` | 16 | 콘텐츠 좌우 여백 |
| `space.xl` | 24 | 섹션 간 |
| `space.2xl` | 32 | 화면 상하 여백 |
| `size.touch.min` | **48** | 모든 인터랙티브 요소 히트 영역 최소 |
| `size.touch.primary` | 56 | 주 액션 버튼 높이 |
| `size.icon.sm/md/lg` | 20 / 24 / 32 | **시각 크기 — 히트 영역과 별개** |

**표준·권고와 설계값의 구분 (혼동 금지)**

| 구분 | 값 | 성격 |
|---|---|---|
| 웹 WCAG 2.5.8 AA | 24×24 CSS px (또는 간격·인라인·대안 예외) | 표준 |
| 웹 WCAG 2.5.5 AAA | 44×44 CSS px | 표준 |
| Android 권고 | 48×48 dp, 간격 8dp 이상 | 플랫폼 권고 |
| iOS 권고 | 기본 44×44 pt, 최소 28×28 pt | 플랫폼 권고 |
| `[KR]` KS 기준 | 24×24 '픽셀' — 플랫폼 환산 방식을 문서에 기록해야 함 | 표준(해석 필요) |
| **이 프로젝트 설계값** | **48×48**, 인접 간격 8 이상 | **팀 결정값 — 표준 아님** |

- 시각 크기가 작아도 **히트 영역을 패딩으로 확장**한다(24 아이콘 + 12 패딩 = 48).
- **목록 행 전체가 탭 대상**이다. 작은 화살표만 누르게 하지 않는다.
- 파괴적 액션은 다른 버튼에서 `space.md` 이상 떨어뜨린다.
- **스와이프에만 있는 기능 금지** — 동일 기능의 버튼을 반드시 함께 제공한다.
- Safe Area Insets를 지킨다. 상호작용 요소에 safe-area 무시를 적용하지 않는다.
- 주 액션은 **하단 고정(엄지 영역)**, 상단에는 되돌릴 수 있는 보조 액션만(뒤로·도움말·닫기). 키보드가 올라올 때 가려지지 않게 한다.
- **파괴적 액션을 상단 우측에만 두지 않는다.**
- 정보 밀도와 충돌하면 **한 화면에 보이는 항목 수를 줄인다.** 타깃을 줄이지 않는다.

### 5.5 모션

| 의미 토큰 | 기본 | 모션 축소 시 |
|---|---|---|
| `motion.press` | 즉각 (가장 짧게) | **그대로 유지 — 절대 제거하지 않는다** |
| `motion.transition` | 화면 전환 | **크로스페이드로 대체** |
| `motion.reveal` | 요소 등장 | 페이드만 |
| `motion.emphasis` | 주의 환기 | **제거** |

- **지속 시간을 이 문서에서 확정하지 않는다.** 150–300ms를 시험 시작 범위로 쓰고 기준 저성능 기기에서 맞춘 뒤 토큰에 기록한다(§4 C-3).
- 모션 축소 시 **지속 시간을 0으로 만들지 않는다** — 화면이 튀면 방향 감각을 잃는다. 페이드로 대체한다.
- `motion.press`는 장식이 아니라 **입력 확인 신호**다. 모션 축소에서도 유지한다.
- **애니메이션이 유일한 상태 신호이면 안 된다.**
- 모션 축소 감지: `[웹]` `prefers-reduced-motion` / `[iOS]` `accessibilityReduceMotion` / `[AOS]` **공식 API 없음** — `ANIMATOR_DURATION_SCALE == 0f` 확인이 관행. 구현 전 최신 문서 재확인.
- 시차(parallax)·자동 캐러셀을 기본으로 두지 않는다. 자동 재생·자동 회전 콘텐츠에는 **정지 수단**을 준다.
- 햅틱은 주요 성공/실패 경로에만. **햅틱만으로 완료를 알리지 않는다.**
- **레이아웃을 흔들지 않는다**: 이미지·배너 크기를 미리 예약, 스켈레톤은 실제 콘텐츠와 같은 높이, 비동기 배너는 콘텐츠 위가 아니라 아래 또는 오버레이. `[웹]` CLS 75분위 0.1 이하.

---

## 6. 문구 기준

### 6.1 금지어 — 자동 검사 대상

문자열 리소스에 대해 린트한다.

| 금지 | 대체 |
|---|---|
| `죄송합니다`, `죄송하지만` | 무엇을 하면 되는지 |
| `유효하지 않은`, `잘못된 값`, `잘못 입력하셨습니다` | 무엇이 어떻게 틀렸는지 |
| `필수 항목입니다` | `전화번호를 입력해 주세요` |
| `오류가 발생했습니다` | 원인 + 다음 행동 |
| `앗`, `이런`, `헉` | 담백한 서술 |
| `~하셔야 합니다`, `~해야만` | `~해 주세요` |
| `간편하게`, `스마트하게`, `쉽고 빠르게` | 삭제 |
| 오류 코드 노출 (`E_`, `0x`, HTTP 상태) | 로그로만. 상담에 필요하면 보조 정보로만 |
| 버튼 라벨 `확인` `제출` `완료` `OK` `계속`(다음 단계가 아닐 때) | 동사 + 목적어 |
| `잠시만 기다려 주세요`(단독) | `예약 내역을 불러오는 중` |
| `곧`, `잠시 후`, `최근`(상태 정보로) | 절대 시각 |
| `데이터가 없습니다`(실패와 부재를 뭉갬) | 부재와 실패를 분리 |

### 6.2 형식

| 종류 | 형식 | 예 |
|---|---|---|
| 화면 제목 | 질문 또는 명사구 | `생년월일을 알려주세요` |
| 버튼 | 동사 + 목적어 | `예약 확정하기` |
| 필드 라벨 | 명사구 | `전화번호` |
| 형식 안내 | 조건만 (회색, 오류 아님) | `숫자 11자리` |
| 오류 | 지시형 / 서술형 | `전화번호를 숫자 11자리로 입력해 주세요` |
| 상태 | 상태어 + 시각 | `병원에 접수됨 · 오후 2시 12분` |
| 빈 상태 | 사실 + 다음 행동 | `아직 예약이 없어요` |
| 진행 | 대상 + 진행형 | `예약 내역을 불러오는 중` |

### 6.3 문장

- 한 문장 **40자 이내** 목표, **한 문장에 한 정보**. 능동태·현재형·직설. 이중 부정·내포절 금지.
- **용어 사전을 1개 유지**하고 앱 전체에서 같은 개념에 같은 단어를 쓴다. `예약`/`신청`/`접수`를 섞어 쓰지 않는다.
- 전문 용어는 **첫 등장에 괄호로 풀어쓴다**: `본인확인(휴대폰으로 신분을 확인하는 절차)`.
- 정확한 명칭이 필요한 말(`임시 저장`, `동의 철회`, `본인확인`)은 유지하고 짧은 풀이를 붙인다. `수진자` 같은 낯선 말은 `진료받을 사람`으로 바꾼다.
- 외래어·약어는 한국어 대체어가 있으면 한국어: `싱크`→`동기화`, `OTP`→`일회용 인증번호(OTP)`.
- 문장 끝을 전부 `~해요`로 바꾸는 것이 개선은 아니다. **정중한 높임말**을 쓰되 유아적 표현·반말은 쓰지 않는다.
- 법정 고지·약관 원문은 유지하되 **요약문을 위에 붙인다.**
- 상담 안내는 **실제 운영시간일 때만** 쓴다: `상담은 평일 오전 9시부터 오후 6시까지 가능해요`.

### 6.4 숫자·날짜·단위 (MUST)

| 종류 | 표기 |
|---|---|
| 날짜 | `2026년 9월 11일(금)` — 목록 축약 시에도 **상세에는 완전 표기** |
| 시간 | `오후 2시 30분` — 24시간제와 오전/오후를 혼용하지 않는다 |
| 상대 시간 | `3일 전 (2026년 9월 1일)` — 절대 시간 병기 |
| 금액 | `12,000원` — 통화 기호만 쓰지 않는다 |
| 수치 + 단위 | `130/85 mmHg` — **단위를 아이콘으로 대체하지 않는다** |
| `[의료]` 검사 수치 | 값 + 단위 + **측정 시점** + **참고 범위**. 수치 하나만 단독으로 띄우지 않는다 |
| `[의료]` 투여량 | `100mg`이 `10mg`으로 잘려 보이지 않도록 **강조 블록으로 분리**. 확대 시에도 무결성 유지 |

- `9/4` 같은 월/일 축약, `2026-09-04T05:00:00Z` 같은 기계 표기 노출 금지.
- 날짜·시간 포맷 함수를 쓴다. 문자열 직접 조합 금지.
- 기기 로케일이 `en_US`여도 한국어 표기 규칙을 유지한다.

---

## 7. 상태 모델 — 모든 것의 기초

### 7.1 부작용 있는 작업의 표준 상태

```
idle → submitting → (queued) → accepted → confirmed
                             ↘ failed
                             ↘ partially_succeeded
                             ↘ unknown (응답 유실)
```

| 상태 | 뜻 | 화면 표기 |
|---|---|---|
| `idle` | 시작 전 | 액션 버튼 활성 |
| `submitting` | 보내는 중 | 눌림 반응 + 진행 표시 + **취소 가능 여부 명시** |
| `queued` | 오프라인 등으로 대기 | **성공도 실패도 아닌 제3의 표기**: `보내는 중(대기)` |
| `accepted` | 서버가 받음, 사람 확인 전 | `병원에 접수됨` + **시각** |
| `confirmed` | 상대가 확정 | `예약 확정 · 9월 11일(금) 오후 2시 30분` |
| `failed` | 도달하지 않음 | **가장 눈에 띄게.** 목록에서도 보임. 재시도 수단 |
| `partially_succeeded` | 일부만 성공 | 성공/실패를 **각각** 표시 |
| `unknown` | 응답 유실로 처리 여부 모름 | `처리 결과를 아직 확인하지 못했어요. 요청 내역을 확인해 주세요.` + 상태 조회 |

**금지**: `success` 하나로 `accepted`와 `confirmed`를 합치는 것. `loading` 하나로 `submitting`과 `queued`를 합치는 것. `unknown`을 `failed`로 표시해 사용자가 재신청하게 만드는 것.

### 7.2 화면 단위 상태 — 6종 전부 구현

`loading` / `empty` / `error` / `offline` / `partial` / `content`

**6개 중 하나라도 슬롯이 없으면 화면 컴포넌트가 미완성이다.** 자동 검사 항목.

### 7.3 데이터 신선도

| 상태 | 표기 |
|---|---|
| `fresh` | 표기 없음 |
| `stale`(캐시) | `2026년 9월 4일 오후 2시 10분 기준` |
| `unavailable`(오프라인 + 안전 관련) | **값을 가리고** `최신 정보를 확인할 수 없어요` |

> "항상 무언가 보여준다"와 "틀린 것을 보여주지 않는다"가 충돌하면 **후자가 우선**한다.

### 7.4 `[의료]` 상태 어휘 고정 — 앱 전체에서 이 단어만 쓴다

| 상태 | 화면 문구 | 되돌릴 수 있나 |
|---|---|---|
| 저장됨 | `임시 저장됨` (아직 보내지 않음) | 예 |
| 전송 중 | `보내는 중` | 취소 가능 여부 명시 |
| 접수됨 | `병원에 접수됨` | 조건부 |
| 확정됨 | `예약 확정 (9월 11일 오후 2시 30분)` | 취소 절차 필요 |
| 실패/미전송 | `전송 실패 — 병원에 전달되지 않았어요` | 재시도 |

`완료`, `처리됨`, `OK`처럼 모호한 단어 금지. **모든 상태에 시각을 붙인다.** "저장"과 "전송"을 같은 버튼으로 처리하지 않는다.

### 7.5 중복 방지 (MUST)

- **클라이언트**: 요청 시작과 동시에 버튼 비활성 + 진행 상태 전환.
- **서버**: **멱등 키**를 받아 동일 키 재요청에 이전 결과를 반환한다.
- 멱등 키는 **사용자 의도 단위**로 생성한다(버튼 탭이 아니라 "이 예약 건"). 화면 재진입·앱 재실행·타임아웃 재시도에서 같은 키가 유지되어야 한다.
- **버튼 비활성화만으로 해결했다고 보지 않는다.** 앱 종료 후 재실행, 뒤로 가기 후 재진입에서 뚫린다.
- 시간 기반 휴리스틱("최근 N초 내 동일 요청 차단")만 쓰지 않는다.
- 멱등 키 방식(IETF Idempotency-Key)은 **Internet-Draft이며 확정 표준이 아니다.** "표준 준수"라고 쓰지 않는다.

### 7.6 서버가 진실의 원천

- 사용자 조작 후 화면은 **서버 응답으로 확정**한다. 실패하면 이전 상태로 되돌리고 이유를 말한다.
- 화면 재진입·앱 재개(foreground) 시 핵심 상태를 다시 조회한다.
- 같은 데이터가 여러 화면에 있으면 하나의 원천에서 파생시킨다(목록과 상세가 어긋나면 안 된다).
- 화면이 닫혔다고 서버 작업이 취소된 것은 아니다. `취소`와 늦은 응답의 순서가 뒤바뀌어도 최신 업무 상태를 기준으로 맞춘다.
- **낙관적 갱신 허용 목록**: 정렬·필터·별표·읽음 표시. **그 외는 금지**(§4 C-6).

---

## 8. 컴포넌트 계약

각 컴포넌트는 **상태 / 문구 / 접근성 / 검증**을 함께 정의한다. 정상 화면만 만들면 미완성이다.

### 8.1 ActionButton (주 액션)
- 상태: `enabled` / `pressed` / `submitting` / `disabled`. **비활성 이유를 제공**한다(`날짜를 선택하면 다음 단계로 갈 수 있어요`).
- 라벨 = **동사 + 목적어**. `submitting` 문구는 라벨의 진행형(`예약하기` → `예약하는 중…`).
- **실행은 업 이벤트에서.** 손가락을 버튼 밖으로 빼면 취소된다.
- 히트 영역 48 이상, 파괴적 액션과 12 이상 이격.
- 로딩 전후 **너비 변화로 오터치를 유발하지 않는다.**
- 금지: 눌림 반응 없이 화면 전환 / 아이콘만 있는 주 액션 / 로딩 중 전체 화면 차단.

### 8.2 FormField (입력)
- 상태: `empty` / `focused` / `filled` / `error` / `disabled` / `readonly`
- 구조: 라벨(항상 보임) → 보조 설명(형식 안내) → 입력 영역 → 오류 문구(error에서만)
- **플레이스홀더를 라벨 대신 쓰지 않는다.** 라벨–입력을 프로그래밍적으로 연결한다.
- `autocomplete` / `autofillHints`로 입력 목적을 식별할 수 있게 한다. 키보드 타입을 값에 맞춘다.
- **붙여넣기·자동완성을 막지 않는다.** 한글 조합 중 검증·자동 이동을 강제하지 않는다.
- **검증 시점 분리**: 형식 힌트는 정적/실시간, **오류 확정은 제출 시.** 필드 이탈만으로 빨간 오류를 띄우지 않는다. 이미 뜬 오류는 고치는 즉시 해제해도 좋다(부정→긍정 전환은 실시간 허용).
- 서버 측 검증은 **항상** 수행한다.
- **입력을 관대하게 받는다**: `010-1234-5678`, `010 1234 5678`, `+82 10-1234-5678` 모두 수용 후 내부 정규화. 길이 초과 시 조용히 잘라내지 않는다.
- 날짜는 선택기 우선 + 직접 입력 허용. **생년월일에 수십 년 달력 이동을 강제하지 않는다.**

### 8.3 ErrorSummary (오류 요약) — MUST
```
입력하신 내용을 확인해 주세요
· 전화번호를 숫자 11자리로 입력해 주세요   → [전화번호 필드로 이동]
· 생년월일을 선택해 주세요                 → [생년월일 필드로 이동]
```
- 화면 **최상단**에 배치. 오류가 **1건이어도 표시**한다.
- 표시와 동시에 **초점을 요약으로 이동**한다. 각 항목을 누르면 해당 필드로 이동하고 초점이 놓인다.
- **요약 문구와 필드 옆 문구가 글자 단위로 동일**해야 한다.
- **사용자 입력값을 모두 보존**한다. 오류 후 폼 초기화 금지.
- 금지: 토스트로만 오류 알리기 / 애니메이션으로 밀어 넣기(레이아웃 이동).

### 8.4 StatusBadge
**아이콘(모양) + 텍스트 + 색 — 세 요소 모두 필수.** 색만 다른 점(dot) 금지. 시각 없는 상태 금지.

### 8.5 ConfirmDialog — 4요소 필수
```
처방전을 ○○약국으로 보낼까요?
보내면 취소할 수 없어요. 다른 약국으로 바꾸려면 병원에 문의해야 합니다.
[○○약국으로 보내기]   [보내지 않기]
```
- 무엇을 / 누구에게 / 언제 / 되돌릴 수 있는지 **모두** 적는다.
- 확인 버튼 라벨은 **행동을 반복**한다. `[확인]` 금지.
- 파괴적 액션과 취소를 **라벨과 위치로** 구분한다(색만으로 X).
- 열면 초점이 안으로, 닫으면 **연 지점으로 복귀**. 시스템 뒤로 가기로 닫을 수 있고 닫으면 아무 일도 일어나지 않는다.
- **자동 닫힘 타이머 금지.** 실행취소 토스트 3초를 유일한 안전장치로 쓰지 않는다(반응이 느린 사용자에게는 안전장치가 아니다).
- `[의료]` 대리 모드에서는 **대상자 이름을 문구에 포함**한다.
- **확인이 필요한 액션 목록(§4 C-4)에 없으면 넣지 않는다.**

### 8.6 EmptyState / ErrorState — 3요소 필수
```
아직 예약이 없어요
진료를 예약하면 여기에 표시됩니다.
[진료 예약하기]   [도움말]
```
- ErrorState는 원인을 구분한다: 네트워크 / 서버 / 권한 / 데이터 없음 — 각각 다른 문구, 다른 다음 행동.
- **재시도 버튼 필수. 도움 진입점 필수.**
- **부재와 실패를 섞지 않는다**: `내역이 없습니다` ≠ `내역을 불러오지 못했어요`.

### 8.7 OfflineBanner
- 화면 **상단 고정**(토스트 아님). 문구는 상태 + 영향: `인터넷에 연결되어 있지 않아요. 저장한 내용은 연결되면 자동으로 보냅니다.`
- 캐시 데이터에 **마지막 갱신 시각** 병기. 재연결 시 자동 재시도 → 성공하면 배너 제거, 실패하면 `[다시 시도]` 유지.
- 배너 삽입이 콘텐츠를 밀지 않게 **자리를 미리 예약**한다.
- 연결이 끊겼다고 앱 전체를 오류 화면으로 덮지 않는다. **오프라인에서 읽기는 가능**하게 캐시하고 쓰기는 큐에 넣는다.
- `[의료]` 의사결정에 직접 쓰이는 값은 오래된 캐시를 **가린다**(§7.3).

### 8.8 ProxyBanner `[의료]`
- 대리 모드의 **모든 화면 상단에 지속 표시.** 스크롤해도 사라지지 않는다.
- 문구: `○○○ 님을 대신해 이용 중` + `[내 계정으로]`
- 색만으로 구분하지 않는다. 앱 재실행 시 **본인 계정으로 초기화**하거나 명시적으로 다시 알린다.
- 진입 시 **부여된 권한 범위를 1회 표시**: `○○○ 님이 나에게 준 권한: 예약 관리, 처방 조회 (진료 기록 열람 없음)`

### 8.9 ListRow
- **행 전체가 탭 대상.** 행 안의 별도 액션 버튼은 12 이상 이격.
- 스와이프 액션이 있으면 **동일 기능의 버튼을 함께** 제공한다.
- 글자 확대 시 **1열 스택으로 전환**한다.
- 목록 아이템은 접근성 트리에서 **하나로 묶여** 한 번에 읽힌다.
- 상세에서 돌아올 때 **스크롤 위치를 유지**한다.

### 8.10 HelpEntry (도움 진입점) — MUST
- **모든 화면에서 동일한 상대적 위치.**
- **사람에게 닿는 경로**(전화 또는 상담 채널)를 최소 1종 포함. 산간벽지·저숙련 사용자에게 마지막 안전망이다.
- 실제 운영시간과 연결 가능 범위를 알린다. 현재 화면 맥락에 맞는 도움을 우선 보여준다.

---

## 9. 접근성 (MUST 묶음)

- **이름·역할·상태**: 모든 인터랙티브 요소에 부여. 화면에 보이는 텍스트를 접근성 이름에 포함한다(음성 명령 사용자가 보이는 말로 조작할 수 있어야 한다). 아이콘 전용 버튼에는 기능을 서술하는 이름(`닫기`, `공유하기`). 장식 이미지는 접근성 트리에서 숨긴다. 버튼 안에 버튼 중첩 금지.
- **200% 확대에서 기능 손실 없음**: 모든 버튼 라벨이 완전히 보이고 모든 기능에 도달 가능. 가로로 좁아지면 **스택 레이아웃으로 전환**. 의미 있는 아이콘은 글자와 함께 커진다. **가로 스크롤 0.** "큰 글씨 모드에서는 일부 기능 제한" 금지.
- **초점**: 순서는 읽는 순서와 일치. 모달·시트가 열리면 초점을 안으로, 닫히면 **연 지점으로 복귀**. 초점 표시가 하단 고정 바에 가려지지 않게. 오류 발생 시 초점을 오류 요약으로. **초점 표시에 시간 제한을 두지 않는다.** `outline: none` 후 대체 표시 없음 금지.
- **색 단독 금지**: 상태·차트·필수 표시·오류 모두 텍스트 또는 모양을 함께 준다. 화면을 회색조로 바꿔도 모든 상태가 구분되어야 한다.
- **제스처 대안**: 제스처·드래그만으로 필수 기능을 실행하게 하지 않는다.
- **앱 내 표시 조절**(SHOULD): `글자 크게 보기`·`고대비` 옵션을 두고 시스템 설정과 **더 큰 쪽**을 적용한다(곱하지 않는다). **시스템 설정 대응(200% 확대)을 먼저 완성한 뒤에** 추가한다. 순서를 바꾸지 않는다.
- 지도·차트 같은 2차원 콘텐츠는 리플로 예외지만 **범례·컨트롤·수치 요약은 텍스트로 확대 가능**해야 한다.

---

## 10. 화면별 적용 — MediVU 구조

> 아래 구조는 `../Connecting-app/구상1.txt` 기준이며 **확정이 아니다.** 구조가 바뀌면 이 절을 함께 고친다.

### 10.1 하단 탭 (홈 / 진료 / Q / 주변 / 메뉴)
- **아이콘만 두지 않는다. 텍스트 라벨 필수.** 햄버거(≡)를 메뉴로 인지하지 못하는 사용자가 있다 → `메뉴` 라벨을 붙인다.
- 현재 탭 표시를 **색만으로** 하지 않는다(모양 또는 텍스트 강조 병행).
- 글자 확대 시 라벨이 잘리지 않게 처리한다(2줄 허용 또는 스택).
- **탭 순서·라벨·아이콘을 함부로 바꾸지 않는다.** 바꾸면 재학습 비용이 크다.

### 10.2 온보딩
- **여러 장 캐러셀 금지.** 첫 과업 안의 짧은 힌트로 대체한다.
- **로그인 없이 볼 수 있는 것을 먼저 보여준다.** 예약하려는 시점에 로그인을 요구한다.
- **첫 실행에 권한을 연달아 요청하지 않는다.** 권한은 처음 필요해지는 화면에서 요청한다.
- 첫 과업 완료 후 **무엇이 일어났고 다음에 무엇을 하면 되는지** 한 화면으로 정리한다.

### 10.3 홈
- **핵심 과업 진입점 1~3개**를 상단에, 텍스트 라벨이 붙은 큰 버튼으로. 나머지는 아래로 **내린다**(숨기지 않는다).
- 라벨은 사용자의 목적어로: `진료 예약하기`, `내 예약 보기`. 내부 도메인 용어 금지.
- **자동 회전 배너·아이콘만 있는 그리드 금지.**
- **통과 조건**: 360×640 화면 + 글자 200%에서 핵심 진입점이 **스크롤 없이** 보인다.
- 재방문자에게: `지난번에 쓰다 만 예약이 있어요 [이어서 하기]`를 상단 카드로. 전면 팝업 금지.

### 10.4 진료 탭 (초진: 새 의료진 / 재진: 기존 의료진)
- **초진/재진 선택은 한 화면에서 하나의 결정.** 두 선택지를 큰 버튼으로.
- 기존 의료진 하위 기능(진료 전 문의 / 진료 신청 / 예약)은 **각각 다른 단어**를 쓴다. `신청`·`접수`·`예약`을 섞지 않는다 → 용어 사전에 등록.
- **`[의료]` 진행 표시 적용 구간**(§4 C-1): 진료 신청·예약 흐름에 텍스트 진행 표시.
- 진료 신청·예약은 **멱등 키 필수**. 화면 진입 시 1회 생성해 재시도·재진입에서 유지한다.
- **낙관적 갱신 금지 구간**: 예약 확정, 처방 전송, 복약 기록.
- 예약 확정·취소·처방 전송은 **확인 대화상자 목록에 포함**한다.
- 예약 일시는 **완전 표기**: `2026년 9월 11일(금) 오후 2시 30분`.
- 병원 자체 예약으로 넘어가는 경우 **외부 앱·웹 복귀 실패의 대안 경로**를 둔다(전화 번호 표시 등).

### 10.5 진료 전 문의 (채팅)
- 의사에게 알림이 가는 기능이므로 **전송 5단계 상태를 그대로 적용**한다. `전송 완료` 하나로 뭉개지 않는다.
- 오프라인이면 **큐에 넣고** `보내는 중(대기)`로 표시한다. 입력 초안은 로컬에 보존한다.
- `[의료]` **알림 본문에 진단명·약품명·수치를 넣지 않는다.** `○○병원에서 새 안내가 있어요` 수준.
- "과도한 질문·진료 대체 질문 자제" 안내는 **사용자를 탓하지 않는 문구**로 쓴다. 금지어 규칙을 그대로 적용한다.
- 답변 대기 상태를 사실대로 표시한다. 근거 없는 예상 시간(`곧 답변드립니다`)을 쓰지 않는다.

### 10.6 Q 탭 (사연·질문–전문가 답변)
- 의사 프로필 → 진료 기능 연결 시 **어느 의료진의 어느 기능인지**를 화면에 남긴다.
- **별표(즐겨찾기)는 낙관적 갱신 허용 목록**에 포함된다. 되돌릴 수 있는 저위험 행동이다.
- 답변 내용이 진료를 대체하지 않는다는 안내를 **한 문장, 담백하게** 둔다.
- 목록·상세 어디서든 **같은 데이터가 다르게 보이지 않게** 한다.

### 10.7 주변 탭 (지도)
- **지도는 리플로 예외**지만 **의료기관 목록·필터·범례는 텍스트로** 제공하고 확대에 대응한다.
- **1·2·3차 의료기관을 색만으로 구분하지 않는다.** 색 + 텍스트 라벨(또는 모양)을 함께 준다.
- `현재 진료 가능` 여부는 **기준 시각을 병기**한다: `오후 2시 10분 기준`.
- 지도 제스처만으로 도달하는 정보를 만들지 않는다. **목록 대안 필수.**
- 위치 권한 거부 시 **지역을 직접 선택**하는 대체 경로를 준다: `가까운 기관을 찾으려면 위치가 필요해요. 지역을 직접 선택할 수도 있어요.`

### 10.8 메뉴 탭
- 자주 묻는 질문(급여/비급여, 1·2·3차 의료기관 등)은 **쉬운 말 + 그림** 방식을 우선한다.
- **로그아웃을 1-depth에 둔다.** 설정 3단계 아래에 숨기지 않는다(공용 기기·보호자 기기 대응).
- 대리 이용 권한 **해지 경로**를 본인 계정 설정에 둔다.
- 도움 진입점(전화 포함)이 여기서도 동일 위치에 있어야 한다.

### 10.9 `[의료]` 대리 이용 (보호자)
- 대리 권한은 **기능 단위로 분리**한다: 예약 관리 / 처방 조회 / 진료 기록 열람 / 결제. **전부-아니면-전무 금지.**
- 권한 부여는 **본인 또는 자격 있는 기관의 확인**을 거친다. 앱 안에서 보호자가 스스로 권한을 만들 수 없다.
- 대리인이 조회·변경한 내역을 **본인이 볼 수 있게** 남긴다.
- 범위 밖 항목은 자리표시자로 가린다: `권한이 없어 볼 수 없어요`. 미리보기·알림에도 노출하지 않는다.
- **가족이라는 이유로 권한을 추정하지 않는다.**
- `USER` 검증 목표: "지금 누구의 정보인가요?" **오답 0건.**

---

## 11. 검수와 출시 게이트

### 11.1 기록 형식

모든 항목을 `PASS` / `FAIL` / `N/A`(사유 필수) / `UNVERIFIED`(확인 예정일·담당 필수) 중 하나로 기록한다. **빈칸을 남기지 않는다.**
`UNVERIFIED`를 `PASS`로 올려 적지 않는다. 자동 검사만 통과한 항목을 `USER` 통과로 적지 않는다.

### 11.2 자동 검사 (CI, 매 커밋)

접근성 린터 / 이름·역할 존재 / 숫자 리터럴 0건 / 글자·lineHeight 단위 / 색 대비 / 금지어 / 히트 영역 / 화면 6상태 슬롯 / 오류 요약 + 초점 이동 / 요약–필드 문구 동일성 / `autocomplete` / 최대 글자 스냅샷 / 멱등성 통합 테스트 / 실패 주입 롤백 / `[의료]` 낙관적 갱신 금지 정적 검사 / `[의료]` 알림 페이로드 민감 필드 / `[의료]` 대리 배너 전 라우트 / `[의료]` 권한 범위 403 / 선택 동의 기본값 `false` / `[웹]` CLS

### 11.3 개발자 수동 점검 (화면 완성 시)

작은 화면(320~360) / 글자 200% 완주 / 스크린리더 완주 / 초점 왕복 5회 / 회색조 / 오프라인 눌림 반응 / 다운 이벤트 실행 없음 / 느린 통신 / 비행기 모드 순회 + 재연결 큐 / 연타 3회 → 결과 1건 / 뒤로 가기 3회 / 강제 종료 후 초안 복원 / 모션 축소 / **권한 전부 거부 상태로 핵심 흐름 완주** / 비밀번호·OTP 자동 채움 / 입력 관대성 / 부분 실패 주입 / 로케일 변경 / `[의료]` 상태 5종 주입 / `[의료]` 대리 모드 순회 + 재실행 / `[의료]` 잠금화면 알림 / 확인 대화상자 4요소

### 11.4 출시 게이트

| 게이트 | 조건 | 결과 |
|---|---|---|
| **G1 안전** `[의료]` | MED·PRX 항목 중 하나라도 `FAIL` 또는 `UNVERIFIED` | **출시 불가** |
| **G2 접근성 MUST** | 대비·터치·이름/역할·200% 확대·초점·색단독·인증·재입력·오류요약·모션축소 중 `FAIL` | **출시 불가** |
| **G3 핵심 과업** | 대상 사용자 검증에서 독립 성공률이 사전 기준 미달 | **출시 불가** |
| **G4 상태 정확성** | 오프라인·부분 성공·멱등·서버 동기화·눌림 반응 중 `FAIL` | **출시 불가** |
| **G5 그 외 MUST** | 나머지 MUST `FAIL` | 원인·완화·기한 기록 시 조건부 출시 |
| **G6 SHOULD** | SHOULD `FAIL` | 사유 기록 후 출시, 다음 주기 재검토 |

**절대 금지**: 시각 디자인 완성도·애니메이션 품질·브랜드 점수로 G1~G4를 상쇄하는 것.

### 11.5 실사용자 검증 항목 (`USER` — 자동 검사로 대체 불가)

핵심 과업 독립 성공률 / 오류 후 독립 복구율 / **상태 이해도("지금 병원이 이걸 알고 있나요?")** / 문구 이해도 / 첫 탭 정확도 / 도움 진입점 발견율 / 날짜 이해도 / 신뢰 / 피로·부담 / **`[의료]` 대리 대상 인지(오답 0건 목표)** / 90일 후 재방문 성공률 / 주기 맞춤 재사용(출시 후 측정)

### 11.6 회귀 검수 (변경 후)

- 변경 화면의 자동 검사 전체 재실행 — 신규 `FAIL` 0건.
- **주 액션 위치·라벨·탭 순서가 바뀌지 않았는지.** 바뀌었으면 재학습 비용을 설명하고 안내 계획을 세운다.
- 상태 어휘가 바뀌지 않았는지. 바뀌었으면 앱 전체 일괄 반영.
- 멱등 키 단위 유지 / `[의료]` 낙관적 갱신 금지 목록 유지.
- **범위 밖 변경 0건** — 무관한 리팩터링·디자인 시스템 교체·기능 추가 금지.

---

## 12. 확정 필요 — 아직 결정하지 않은 것

작업 중 이 항목에 부딪히면 **임의로 가정하지 말고 사람에게 묻는다.**

| # | 항목 | 왜 막혀 있나 | 필요한 것 |
|---|---|---|---|
| 12-1 | 본문 기본 크기 17 vs 18 | 조사본이 갈린다(§4 C-2) | 실기기에서 한국어 읽기·확대 비교 시험 |
| 12-2 | **기준 저성능 기기 미지정** | 모션·성능 예산을 확정할 수 없다 | 모델명·OS 버전을 문서에 기록 |
| 12-3 | KS X 3253 원문 미대조 | Claude 조사본은 2016판 미확인, Codex 조사본은 2025 개정판 확인 — 판본이 어긋난다 | 2025 개정판 원문 대조. 그 전까지 "KS 준수" 표기 금지 |
| 12-4 | `[KR]` 개인정보 보호법 제23조 조문 원문 미열람 | 민감정보 동의 UI를 확정할 수 없다 | **법무 검토 필수.** 검토자·일시를 기록 |
| 12-5 | 한국의 대리 접근 제도·법정대리인 요건 | 근거가 영국 NHS 사례뿐이다 | 국내 제도 조사 + 법무 검토 |
| 12-6 | 기술 스택 미확정 | 토큰 구현 위치·린트 도구를 정할 수 없다 | `../Connecting-app/reference-research/*/STACK_OPTIONS.md` 검토 후 결정 (RN + TS + Expo가 양쪽 조사본의 1안) |
| 12-7 | 기존 디자인 시스템 없음 | 토큰 이름 체계가 이 문서 제안값뿐이다 | 채택 시점에 이름 체계 확정. 기존 시스템이 생기면 **그쪽이 정본** |
| 12-8 | G3 독립 성공률 합격 기준 미정 | 출시 판정을 할 수 없다 | 팀이 **사전에** 수치를 정하고 문서화 |
| 12-9 | 화면 구조 (`../Connecting-app/구상1.txt`) 미확정 | §10이 임시다 | 구조 확정 시 §10 갱신 |
| 12-10 | **사용자 테스트 0건** | 모든 규칙이 가설 상태다 | `../Connecting-app/design-research/Claude/RESEARCH_REPORT.md` §7 검증 계획 실행 |

---

## 13. 작업 보고 형식

UI 작업을 끝낼 때 아래를 적는다.

```
## 바꾼 사용자 경험
(무엇이 사용자에게 달라졌는가)

## 적용한 규칙과 이유
(CORE-xx / NAV-xx / MED-xx … + 왜)

## 의도적으로 어긴 규칙
(규칙 ID / 사유 / 대안 / 재검토 시점)

## 검증
AUTO: (실행한 검사와 결과)
DEV:  (실제로 확인한 환경과 절차)
USER: (없으면 "사용성 미검증"이라고 명시)

## 남은 위험·미검증
(UNVERIFIED 항목과 확인 예정일)

## 출시 차단 여부
```

**금지**: 자동 검사 통과나 LLM 검토를 사용성 검증으로 보고하는 것. 중요한 오처리·데이터 손실·핵심 접근 차단을 남긴 채 "완료"라고 하는 것. 합의한 통과 조건을 충족한 뒤 추가 미화·범위 확대를 계속하는 것.

---

## 14. 막히면

추측하지 않는다. 특히 **대상 사용자 조건, 의료 기능 범위, 법령 요구, 기존 디자인 시스템 유무**는 임의로 가정하지 않고 **묻는다.**

충돌 해결 순서: **법령·안전 → 접근성 MUST → 과업 완료 가능성 → 학습·기억 부담 감소 → 시각·브랜드**
(4·5번을 이유로 1·2·3번을 낮출 수 없다.)
