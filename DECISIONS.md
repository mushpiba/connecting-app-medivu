# 결정 기록

새로 시작하는 판이다. 왜 이렇게 시작했는지만 적는다. 늘어나면 파일을 나눈다.

조사 근거는 저장소 밖에 있다 — `../Connecting-app/reference-research/`(레퍼런스·스택),
`../Connecting-app/design-research/`(디자인 규칙), `../Connecting-app/구상1.txt`(화면 구상).

---

## D-1 · 스택은 React Native + Expo

Claude·Codex 두 조사가 각각 같은 결론에 도달했다.

- 우리가 만들려는 것과 가장 비슷한 앱이 이 스택으로 이미 돈다 — **닥터나우 6.4.0 = RN 0.78.3**
  (APK 해체본 직접 판독). 통화·본인인증·푸시 전부 RN 생태계에 이미 있다.
- 이전 PWA를 막았던 벽(**iOS WKWebView에 `SpeechRecognition`이 없다**)이 여기서 뚫린다.
- 네이티브 뷰로 그리므로 OS 글자 크기와 스크린리더가 기본 동작한다. 접근성 규칙을 지킬 수 있는 스택이다.
- EAS로 macOS 없이 iOS 빌드·서명·제출이 된다. 만드는 사람이 1명이다.

**아직 정하지 않은 것**: 화상 통화 SDK, 음성 전사 방식(온디바이스/서버), 서버, OTA 도입 시점.
전부 해당 기능에 착수할 때 정한다.

## D-2 · 만들지 않는 것 셋 — 나중에 덜어낼 수 없어서

나머지는 다 만들고 규제에 맞춰 조정한다. 이 셋만 처음부터 없다.

| 무엇 | 왜 나중에 못 덜어내나 |
| --- | --- |
| 평점·후기·가격 | **데이터가 쌓인다.** 나중에 없애면 사용자와의 약속을 깨는 일이 된다 |
| 진단명·질병 확률·중증도 | 넣는 순간 **의료기기 인허가 대상**이 된다. Ada Health가 CE 마크를 받아야 했던 이유다 |
| 플랫폼이 만든 의사 목록·정렬 | 덜어내기가 아니라 **제품의 축을 바꾸는 일**이다 |

세 번째의 구체적 형태: **의사는 답변에 붙어서만 등장한다.** 목록이 되는 것은 사용자가 별표한 것과
진료받은 적 있는 곳뿐이다. 정렬은 최신순 하나로 고정하고 그 사실을 화면에 쓴다.

## D-3 · 탭 다섯 · 진료 탭은 「무엇을 하려는가」로 연다

구상1의 `홈 · 진료 · Q · 주변 · 메뉴`를 그대로 쓴다.

`진료` 탭에서 **초진/재진을 환자에게 직접 묻지 않는다.** 그 분기는 법이 만든 것이고
(같은 의료기관에서 같은 증상으로 대면 진료받은 이력이 원칙), 판정 기준이
**「이 앱에서 진료본 적 있는가」가 아니라 「그 의료기관에서 대면 진료받았는가」**라
앱 이력만으로 판정할 수 없다. 그래서 화면은 환자의 말로 묻는다 —
「지금 물어보고 싶어요 / 지금 진료받고 싶어요 / 나중에 갈 병원을 잡고 싶어요 / 어느 과인지 모르겠어요」.

## D-4 · 지도는 v1에 없다

국내 1위 비대면진료 앱조차 네이티브 지도 SDK를 싣지 않았다(해체본 확인).
`주변`은 목록으로 열고, 위치 확인은 기기의 지도 앱으로 넘긴다.
이 결정이 스택을 지도에 종속시키지 않는다.

## D-5 · 디자인 토큰은 `src/theme/tokens.ts`가 정본

화면 코드는 토큰 **이름**만 쓴다. 숫자를 직접 넣지 않는다.
본문 17 · 터치 히트 영역 48 · 눌림 반응은 모션 축소 설정에서도 유지.
근거와 예외는 `design-research/Claude/DESIGN_RULES.md`.

**모션 지속 시간은 아직 임시값이다.** 기준으로 삼을 저성능 기기를 정하지 않았다.

---

## 아직 안 닫힌 것

| # | 무엇 | 상태 |
| --- | --- | --- |
| 1 | **출시 주체** — 양대 스토어 모두 의료 앱을 개인 개발자 명의로 받지 않는다 (Apple 5.1.1(ix) / Google organization + D-U-N-S) | 사용자 판단 중. 개발은 막지 않지만 제출을 막는다 |
| 2 | **Q → 의사 → 진료 연결의 적법성** — 네이버 엑스퍼트는 법무·세무는 유료 상담을 열면서 의료만 뺐다. 이유를 확인하지 못했다 | 제품의 축이자 최대 미검증 리스크 |
| 3 | **2026-12-24 시행 개정 의료법** — 비대면진료 중개매체 신고제·인증제. 하위법령 미공개 | 2026년 하반기 재조사 필요 |
| 4 | **기술 탐침 미실시** — EAS iOS 빌드, 한국어 전사, 본인인증, 화상 | 화면이 붙기 전에 도는지 봐야 한다 |
